# 随想：直接写 Markdown

网站已改为静态 Markdown 卡片。无需启动 services/thoughts、设置密码或部署 API；历史后端代码留作归档，不参与页面运行。

## 添加一张卡片

在 `docs/随想/第一阶段/` 新建文件，例如 `2026-09-27.md`：

```md
---
title: 今天的一点想法
date: "2026-09-27"
stage: 第一阶段
tags: [日常]
thought: true
template: thought-post.html
comments: false
hide: [navigation, toc]
---

今天想记下的事情。

![配图](images/example.jpg)
```

- 每个 `thought: true` 的 Markdown 文件对应一张卡片。标题、时间、标签直接读取元数据，不用 AI 猜测。
- `date` 使用引号包裹的 ISO 日期，也支持 `"2026-09-27 21:30"`；按时间倒序。
- 文本不超过 220 字时卡片展示全部提取文本，长文展示原文前 220 字；“阅读全文”打开原 Markdown 正文。不会生成改写摘要。
- 可用 `<!-- more -->` 提前结束卡片摘录；卡片会显示标记前的第一张图。图片路径相对于该 Markdown 文件，正文照常支持 Markdown。
- 卡片列表带月份筛选，每次显示 12 条。禁用 JavaScript 仍能阅读全部卡片和独立文章。
- `第一阶段` 指本次整理的批次，不是额外推断的人生阶段。以后新增阶段目录时，在 mkdocs.yml 的 not_in_nav 中补上目录模式，避免单篇记录占满导航。
- 临时草稿请放在 `docs/` 之外；当前没有密码保护的“私密草稿”功能。放进 docs 的内容会公开构建。
- 保存后本地重新构建/预览；提交并推送到 main 后沿用既有 GitHub Pages 工作流发布。本次未推送。

## 第一阶段整理边界

本次从无发送人信息的群记录中，按本人确认的两条连续线索提取 138 篇，卡片日期为 2025-09-08 至 2026-09-25，末条正文还保留 9.26 的后续安排。

- 保留原话、语气、表情、原有拼写和未完成的括号。只调整 Markdown 标题、列表空格及段落。
- “今天完成了”“明天打算做”等原始区分保留；原文没有明确状态的相邻日期条目保留在同一篇，不把它们改写成已完成事项。
- 未写年份的 9 月开学段按前后文补记为 2025 年；6–7 月及后段 9 月按前后文补记为 2026 年，文章中已注明。
- 2025-09-18、2025-10-04、2026-07-20 存在同日多条，分别保留；10.04 的“国庆第五天”不擅自改成 10.05。
- “2025-12-23｜做实验设计”以数电实验为主，归属不确定，暂不收录。相邻群友的数分/高代记录、聊天回复和其他打卡均不并入。
- 原始链接保留历史地址，不擅自替换成现在的网站地址。
- 每篇元数据保留 `source_header` 和 `source_lines`。逐篇溯源清单在 `content-audits/thoughts-phase-1.json`，原群全文不复制到公开站点。

## 验证

```powershell
uv run --with mkdocs-material --with pytest python -m pytest tests/test_static_thoughts.py -q
uv run --with mkdocs-material mkdocs build --strict
```

若要再次与原附件逐字校验，将其路径放入 `THOUGHTS_SOURCE` 环境变量，再运行上述测试。校验会检查源文件 SHA-256、行号、正文（只忽略排版空格与插入的标题标记）、元数据和条目数。
