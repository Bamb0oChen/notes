# Light background provenance

Generated with the built-in imagegen tool. Only the original's leftmost 120 × 851 pixels were supplied to the generator, never the character/full image. Mobile is a byte-for-byte copy of the supplied 1200 × 851 JPEG. Desktop is a lossless 1702 × 851 PNG assembled with `compose-light-background.ps1`: 502 new pixels on the left, a 40-pixel seam blend within the authorized 120-pixel overlap, and all remaining original pixels unchanged. Dark assets are unchanged.

## Final generation prompt

Use case: compositing / background outpainting. Input image is ONLY a 120 x 851 pixel left-edge strip of an illustration; it is the edit anchor, NOT a complete scene to stretch. Create a portrait-format background patch, aspect ratio 622:851. Place this original narrow strip aligned at the RIGHT edge of the new patch, full height, occupying exactly 120/622 (19.29%) of patch width. Extend the environment only to its LEFT by 502 units. The patch will be joined at its right edge to the untouched full original illustration, so maintain the exact vertical alignment of all floor/wall boundaries, diagonal window shadows, colors and lighting within the rightmost 19.29%. Scene: gently sunlit empty school hallway seen at a downward angle, pale warm cream wall in upper roughly 60%, honey-colored wooden plank floor in bottom roughly 40%, soft large diagonal window-frame shadows continuing naturally from the anchor. Soft painterly anime visual novel background, matching the reference's slightly hazy warm daylight and muted contrast. Extend those existing wall, floor and shadow planes continuously; no extra objects or new focal point. No characters, no people, no furniture, no lettering, no border, no watermark. Do not repeat or mirror the anchor. Do not darken or recolor the anchor. Output only the seamless environmental patch.
