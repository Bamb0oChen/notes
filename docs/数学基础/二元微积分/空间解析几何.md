直线的解析式

过
$ (x_0,y_0,z_0) $
方向向量为
$(x_n,y_n,z_n)$
所以直线方程

$$
l : (x,y,z) = (x_0,y_0,z_0) + t(x_n,y_n,z_n)
$$

## 中点问题
中点问题的解决有很稳的方法
方法
最稳的方法：
1. 先把两条直线都写成参数式；
2. 写中点 M 的坐标；
3. 用三个坐标式消去参数。
如果眼力足够快，也可以直接看两个方向向量$\vec{d_1},\vec{d_2}$张成一个平面，中点轨迹平面的法向量与$\vec{d_1}\times\vec{d_2}$

## 叉乘
